class Solution:
    def shiftGrid(self, grid: List[List[int]], k: int) -> List[List[int]]:

        def flatten(g):
            pass

        def shift(g):
            pass

        def unflatten(g):
            pass

        
        flattened = flatten(grid)
        shifted = shift(flatten)
        unflattened = unflatten(shifted)
        return unflattened
        
