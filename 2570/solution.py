from collections import defaultdict
from itertools import chain

class Solution:
    def mergeArrays(self, nums1, nums2):
        res = []
        n = len(nums1)
        m = len(nums2)

        i = j = 0

        while i < n and j < m:


        return res
